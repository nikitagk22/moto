# 🚀 Быстрый старт

## Минимальная инструкция для начала работы

### 1️⃣ Проверка требований

```bash
# Windows + Python 3.7+ (32-бит рекомендуется)
python --version
```

### 2️⃣ Настройка config.py

Откройте `config.py` и укажите путь к DLL:

```python
J2534_DLL_PATH = r"C:\Program Files (x86)\OpenECU\OpenPort 2.0\drivers\openport 2.0\openport.dll"
```

**Где найти DLL:**
- Стандартный путь после установки драйверов OpenPort 2.0
- Проверьте: `C:\Program Files\Tactrix\OpenPort 2.0\openport.dll`
- Или найдите файл `openport.dll` на вашем компьютере

### 3️⃣ Подключение оборудования

1. ✅ Подключите Tactrix OpenPort 2.0 к компьютеру (USB)
2. ✅ Подключите адаптер к OBD-II порту мотоцикла
3. ✅ Включите зажигание (не заводя двигатель)

### 4️⃣ Первый запуск - чтение VIN

```bash
python main.py --read-vin
```

**Ожидаемый результат:**
```
============================================================
Начало подключения к Harley-Davidson XG750A
============================================================
✅ Подключение успешно!

------------------------------------------------------------
🔍 Чтение VIN...
✅ VIN: 1HD4LE218KC123456

🎯 VIN: 1HD4LE218KC123456
```

### 5️⃣ Поиск одометра (пробега)

```bash
python main.py --scan
```

**Что это делает:**
- Сканирует DIDs в диапазоне 0xF191 - 0xF19F
- Показывает все доступные параметры
- Предлагает возможные интерпретации значений

**Пример вывода:**
```
✅ DID 0xF192: 00012840 (4 байт)
   ➡️ 4-byte (BE): 75840 (может быть 7584.0 km с коэф. 0.1)
   ➡️ 4-byte (BE): 758.40 km с коэф. 0.01
```

**Сравните с приборной панелью:**
- Найдите DID, значение которого соответствует пробегу на панели
- Запомните этот DID для дальнейшего использования

### 6️⃣ Чтение конкретного одометра

После определения DID:

```bash
python main.py --read-odometer 0xF192
```

### 7️⃣ Полная диагностика

```bash
python main.py --read-vin --scan --ecu-info
```

## 🆘 Проблемы?

### ❌ "Не удалось загрузить DLL"
```bash
# Проверьте путь:
dir "C:\Program Files (x86)\OpenECU\OpenPort 2.0\drivers\openport 2.0\openport.dll"

# Или найдите файл:
dir /s /b C:\*.dll | findstr openport
```

### ❌ "Устройство не подключено"
1. Проверьте USB-подключение (должен загореться индикатор на адаптере)
2. Проверьте OBD-II соединение
3. Убедитесь, что зажигание включено

### ❌ "Timeout ожидания UDS ответа"
Попробуйте альтернативные CAN ID в `config.py`:
```python
UDS_REQUEST_ID = 0x18DA10F1
UDS_RESPONSE_ID = 0x18DAF110
```

## 📚 Дополнительные возможности

### Подробный вывод (отладка)
```bash
python main.py --read-vin --verbose
```

### Сканирование конкретного диапазона
```bash
python main.py --scan --scan-range 0xF180 0xF1FF
```

### Примеры использования в коде
```bash
python example_usage.py
```

## 💡 Полезные советы

1. **Всегда начинайте с `--read-vin`** - это простейший тест связи
2. **Сохраняйте логи** - установите `LOG_TO_FILE = True` в `config.py`
3. **32-бит Python** работает надежнее с DLL
4. **Не трогайте зажигание** во время диагностики
5. **Записывайте найденные DIDs** для повторного использования

## 📞 Справка

```bash
python main.py --help
```

## 🎓 Следующие шаги

1. ✅ Прочитайте VIN
2. ✅ Найдите DID одометра через сканирование
3. ✅ Сохраните найденный DID в `config.py`
4. ✅ Изучите `example_usage.py` для интеграции в свои скрипты
5. ✅ Прочитайте полную документацию в `README.md`

---

**Готово!** Теперь вы можете читать диагностические данные с вашего Harley-Davidson XG750A! 🏍️
