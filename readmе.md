# Harley-Davidson XG750A Diagnostic Tool

Комплексный Python-скрипт для низкоуровневой диагностики Harley-Davidson XG750A с использованием Tactrix OpenPort 2.0 и J2534 PassThru API.

## 🔧 Технические характеристики

- **Адаптер**: Tactrix OpenPort 2.0 (J2534 PassThru)
- **Протоколы**: UDS (ISO 14229), ISO-TP (ISO 15765-2), CAN 2.0
- **CAN скорость**: 500 кбит/с
- **Платформа**: Windows (32-бит Python рекомендуется)

## 📚 Возможности

✅ Чтение VIN (идентификационного номера)
✅ Сканирование DIDs для поиска одометра (пробега)
✅ Чтение конкретных параметров по DID
✅ Чтение информации о ЭБУ
✅ Автоматическое управление диагностической сессией (Extended Diagnostic Session)
✅ TesterPresent keep-alive для поддержания сессии
✅ Поддержка многокадровых сообщений (ISO-TP)

## 💻 Установка

### Предварительные требования

1. **Windows OS** (требуется для J2534 DLL)
2. **Python 3.7+** (32-битная версия рекомендуется для совместимости с DLL)
3. **Tactrix OpenPort 2.0** с установленными драйверами
4. **Доступ к OBD-II порту** Harley-Davidson XG750A

### Установка драйверов OpenPort 2.0

1. Скачайте драйверы с [tactrix.com](https://www.tactrix.com/index.php?option=com_content&view=article&id=23)
2. Установите драйверы и J2534 PassThru DLL
3. Подключите адаптер к компьютеру через USB

### Установка скрипта

```bash
# Скопируйте все файлы в одну директорию
cd /path/to/diagnostics

# Никакие дополнительные зависимости не требуются!
# Используются только встроенные библиотеки Python
```

## ⚙️ Конфигурация

Откройте файл `config.py` и настройте параметры:

### 1. Путь к J2534 DLL

```python
# Укажите путь к установленной DLL OpenPort 2.0
J2534_DLL_PATH = r"C:\Program Files (x86)\OpenECU\OpenPort 2.0\drivers\openport 2.0\openport.dll"
```

Возможные пути:
- `C:\Program Files (x86)\OpenECU\OpenPort 2.0\drivers\openport 2.0\openport.dll`
- `C:\Program Files\Tactrix\OpenPort 2.0\openport.dll`
- `openport.dll` (если DLL в PATH)

### 2. CAN параметры (опционально)

```python
CAN_BAUDRATE = 500000  # 500 кбит/с (стандарт для Harley)
UDS_REQUEST_ID = 0x7E0  # Адрес запроса
UDS_RESPONSE_ID = 0x7E8  # Адрес ответа
```

## 🚀 Использование

### Подготовка

1. Подключите Tactrix OpenPort 2.0 к компьютеру (USB)
2. Подключите адаптер к OBD-II порту мотоцикла
3. Включите зажигание (не запуская двигатель)

### Основные команды

```bash
# Чтение VIN
python main.py --read-vin

# Сканирование DIDs для поиска одометра
python main.py --scan

# Чтение конкретного DID
python main.py --read-odometer 0xF192

# Чтение информации о ЭБУ
python main.py --ecu-info

# Полная диагностика (все данные)
python main.py --read-vin --scan --ecu-info

# Подробный вывод (DEBUG)
python main.py --read-vin --verbose

# Сканирование конкретного диапазона DIDs
python main.py --scan --scan-range 0xF191 0xF1A0
```

### Пример вывода

```
╭────────────────────────────────────────────────────────────╮
│     Harley-Davidson XG750A Diagnostic Tool                    │
│     Tactrix OpenPort 2.0 + J2534 + UDS/ISO-TP                │
╰────────────────────────────────────────────────────────────╯

============================================================
Начало подключения к Harley-Davidson XG750A
============================================================
✅ Подключение успешно!

------------------------------------------------------------
🔍 Чтение VIN...
✅ VIN: 1HD4LE218KC123456

🎯 VIN: 1HD4LE218KC123456

============================================================
✅ Диагностика завершена успешно!
============================================================
```

## 📝 Структура проекта

```
diagnostics/
├── config.py              # Конфигурация
├── j2534_constants.py     # Константы и структуры J2534
├── j2534_wrapper.py       # Обертка J2534 PassThru API
├── isotp_handler.py       # ISO-TP транспортный протокол
├── uds_client.py          # UDS клиент
├── harley_diagnostics.py  # Основной класс диагностики
├── main.py                # Точка входа / CLI
└── README.md              # Документация
```

## 🔍 Поиск одометра (пробега)

Точный DID для одометра Harley-Davidson является проприетарным и не раскрывается публично. Для поиска:

1. **Используйте сканирование**:
   ```bash
   python main.py --scan
   ```

2. **Анализируйте результаты**: Скрипт автоматически предложит возможные интерпретации для каждого найденного DID:
   - 2/3/4-байтовые значения (Big Endian)
   - С коэффициентами масштабирования (0.1, 0.01)
   - Перевод в мили

3. **Сравните с фактическим пробегом**: Найдите DID, значение которого соответствует показаниям на приборной панели

4. **Сохраните DID**: После определения используйте:
   ```bash
   python main.py --read-odometer 0xF192  # Пример
   ```

## 🐛 Устранение неполадок

### Ошибка: "Не удалось загрузить DLL"

✅ **Решения**:
- Проверьте путь к DLL в `config.py`
- Убедитесь, что драйверы OpenPort 2.0 установлены
- Попробуйте 32-битную версию Python

### Ошибка: "Устройство не подключено"

✅ **Решения**:
- Проверьте USB-подключение адаптера
- Проверьте подключение к OBD-II порту
- Убедитесь, что зажигание включено

### Ошибка: "Timeout ожидания UDS ответа"

✅ **Решения**:
- Проверьте CAN ID в `config.py` (UDS_REQUEST_ID, UDS_RESPONSE_ID)
- Попробуйте альтернативные адреса (0x18DA10F1 / 0x18DAF110)
- Увеличьте ISO_TP_TIMEOUT в `config.py`

### Отладка

Для подробной отладки используйте:
```bash
python main.py --read-vin --verbose
```

Или измените `LOG_LEVEL` в `config.py`:
```python
LOG_LEVEL = 'DEBUG'
LOG_TO_FILE = True
```

## 📚 Технические детали

### Реализованные протоколы

1. **J2534 PassThru API**
   - PassThruOpen / PassThruClose
   - PassThruConnect / PassThruDisconnect
   - PassThruReadMsgs / PassThruWriteMsgs
   - PassThruStartMsgFilter (фильтры Flow Control)
   - Фоновое чтение сообщений

2. **ISO-TP (ISO 15765-2)**
   - Single Frame (SF)
   - First Frame (FF)
   - Consecutive Frame (CF)
   - Flow Control (FC)
   - Автоматическая сегментация/сборка

3. **UDS (ISO 14229)**
   - DiagnosticSessionControl (0x10)
   - TesterPresent (0x3E)
   - ReadDataByIdentifier (0x22)
   - Обработка Negative Response Codes (NRC)

### Стандартные DIDs

| DID    | Описание                          |
|--------|-------------------------------------|
| 0xF190 | VIN (гарантировано 17 байт)       |
| 0xF18C | ECU Serial Number                   |
| 0xF191 | Hardware Number                     |
| 0xF192 | Software Number / Calibration ID    |
| 0xF194 | Supplier ID                         |
| 0xF195 | Date of Manufacture                 |
| 0xF197 | System Name                         |
| 0xF19E | Active Diagnostic Session           |

## ⚠️ Предупреждения

- Этот скрипт предназначен **только для чтения** данных
- **Не изменяйте** данные ЭБУ без соответствующих знаний
- Использование на **свой риск**
- Рекомендуется тестировать на **нерабочем мотоцикле**

## 🔗 Полезные ссылки

- [Tactrix OpenPort 2.0](https://www.tactrix.com/)
- [SAE J2534 Standard](https://www.sae.org/standards/content/j2534-1_202104/)
- [ISO 14229 (UDS)](https://en.wikipedia.org/wiki/Unified_Diagnostic_Services)
- [ISO 15765-2 (ISO-TP)](https://en.wikipedia.org/wiki/ISO_15765-2)

## 📝 Лицензия

MIT License - используйте на свое усмотрение, но без гарантий.

## 👏 Благодарности

Основано на техническом отчете о работе с HDLAN/UDS протоколом Harley-Davidson.
