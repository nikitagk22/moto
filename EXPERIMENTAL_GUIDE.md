# 🔬 Руководство по экспериментальному поиску параметров

## Что нужно подбирать методом эксперимента?

Для работы с Harley-Davidson XG750A через OpenPort 2.0 необходимо **экспериментально определить**:

---

## 1️⃣ Путь к J2534 DLL (обязательно)

### Что это?
Это драйвер OpenPort 2.0, через который Python общается с адаптером.

### Стандартные пути:
```
C:\Program Files (x86)\OpenECU\OpenPort 2.0\drivers\openport 2.0\openport.dll
C:\Program Files\Tactrix\OpenPort 2.0\openport.dll
C:\Program Files (x86)\Tactrix\OpenPort 2.0\openport.dll
```

### Как найти:
```bash
# Windows: найти файл openport.dll
dir /s /b C:\*.dll | findstr openport
```

### Где указать:
В файле `config.py`:
```python
J2534_DLL_PATH = r"C:\ваш\путь\к\openport.dll"
```

---

## 2️⃣ CAN ID адреса (если стандартные не работают)

### Что это?
Адреса для общения с ЭБУ по CAN-шине:
- **REQUEST_ID** - адрес, на который мы отправляем запросы
- **RESPONSE_ID** - адрес, с которого приходят ответы

### Стандартные значения (попробуйте сначала):
```python
UDS_REQUEST_ID = 0x7E0   # Тестер → ЭБУ
UDS_RESPONSE_ID = 0x7E8  # ЭБУ → Тестер
```

### Альтернативные значения для Harley-Davidson:
```python
# Вариант 1 (Extended ID):
UDS_REQUEST_ID = 0x18DA10F1
UDS_RESPONSE_ID = 0x18DAF110

# Вариант 2:
UDS_REQUEST_ID = 0x7DF   # Функциональный адрес
UDS_RESPONSE_ID = 0x7E8
```

### Как понять, что адреса неправильные?
Вы увидите ошибку:
```
❌ Timeout ожидания UDS ответа
```

### Как подобрать:
1. Запустите с `--verbose` для просмотра всех CAN сообщений
2. Попробуйте альтернативные адреса из списка выше
3. Скрипт автоматически попробует разные варианты (см. улучшенный код)

---

## 3️⃣ DID одометра (ГЛАВНЫЙ ЭКСПЕРИМЕНТ)

### Что это?
**DID (Data Identifier)** - это идентификатор параметра в ЭБУ.
- VIN имеет стандартный DID: **0xF190** (всегда работает)
- Одометр (пробег) имеет **проприетарный DID** - нужно найти!

### Почему нужно искать?
Harley-Davidson **не публикует** список всех DIDs. Пробег хранится в одном из DIDs в диапазоне **0xF191 - 0xF19F**.

### Как найти DID одометра:

#### Шаг 1: Сканирование
```bash
python main.py --scan
```

Это отсканирует DIDs 0xF191-0xF19F и покажет все доступные параметры.

#### Шаг 2: Анализ результатов

Пример вывода:
```
✅ DID 0xF191: 48443148443331 (7 байт)
   ➡️ 3-byte (BE): 4737841 km (или 473784.1 km с коэф. 0.1)
   ➡️ ASCII: 'HD1HD31'

✅ DID 0xF192: 00012840 (4 байт)
   ➡️ 4-byte (BE): 75840 (может быть 7584.0 km с коэф. 0.1)
   ➡️ 4-byte (BE): 758.40 km с коэф. 0.01

✅ DID 0xF193: 01234567 (4 байт)
   ➡️ 4-byte (BE): 19088743
   ➡️ 4-byte (BE): 1908874.3 km с коэф. 0.1
```

#### Шаг 3: Сравнение с приборной панелью

**ВАЖНО:** Посмотрите на одометр на приборной панели мотоцикла!

Например, панель показывает: **7584 km**

Тогда правильный DID: **0xF192** с интерпретацией "7584.0 km с коэф. 0.1"

#### Шаг 4: Проверка
```bash
python main.py --read-odometer 0xF192
```

Если значение совпадает с панелью - **вы нашли правильный DID!** 🎉

---

## 4️⃣ Коэффициент масштабирования одометра

### Что это?
Данные в ЭБУ могут храниться с разными коэффициентами:

| Коэффициент | Пример сырых данных | Реальный пробег |
|-------------|---------------------|-----------------|
| 1           | 7584                | 7584 km         |
| 0.1         | 75840               | 7584.0 km       |
| 0.01        | 758400              | 7584.00 km      |

### Как определить:
Скрипт автоматически показывает все варианты. Выберите тот, который совпадает с панелью.

---

## 5️⃣ Единицы измерения (км или мили)

### Проверка:
Если ваш мотоцикл показывает пробег в **милях**, нужно конвертировать:

```
мили × 1.60934 = км
км × 0.621371 = мили
```

Скрипт автоматически показывает оба варианта.

---

## 📝 Сохранение найденных параметров

После того как вы **экспериментально нашли** правильные значения, сохраните их в `config.py`:

```python
# Пример найденных параметров
J2534_DLL_PATH = r"C:\Program Files (x86)\OpenECU\OpenPort 2.0\drivers\openport 2.0\openport.dll"

UDS_REQUEST_ID = 0x7E0    # Если стандартный работает
UDS_RESPONSE_ID = 0x7E8

DIDS = {
    'VIN': 0xF190,
    'ODOMETER': 0xF192,  # ← Ваш найденный DID!
    # ...
}

# Настройки одометра
ODOMETER_DID = 0xF192           # Ваш найденный DID
ODOMETER_SCALE_FACTOR = 0.1     # Коэффициент (1, 0.1 или 0.01)
ODOMETER_UNIT = 'km'            # 'km' или 'miles'
```

---

## 🎯 Пошаговый план эксперимента

### День 1: Базовая связь
```bash
# 1. Проверка подключения
python main.py --read-vin

# Если работает - переходим к следующему шагу
# Если не работает - пробуем альтернативные CAN ID
```

### День 2: Поиск одометра
```bash
# 2. Сканирование всех DIDs
python main.py --scan --verbose > scan_results.txt

# 3. Анализ результатов (сравнение с панелью)
# 4. Проверка найденного DID
python main.py --read-odometer 0xF192  # Ваш найденный DID
```

### День 3: Сохранение и автоматизация
```bash
# 5. Сохранение параметров в config.py
# 6. Тестирование
python main.py --read-vin --read-odometer 0xF192
```

---

## ⚠️ Важные замечания

1. **VIN всегда работает** (DID 0xF190) - используйте для проверки связи
2. **Одометр может быть в нескольких DIDs** - выберите тот, что совпадает с панелью
3. **Не все DIDs доступны** - это нормально, ЭБУ может вернуть ошибку
4. **Extended Diagnostic Session** иногда нужна для доступа к защищенным DIDs
5. **TesterPresent** обязателен - иначе сессия закроется через 5 секунд

---

## 🔍 Расширенное сканирование

Если в стандартном диапазоне (0xF191-0xF19F) не нашли одометр:

```bash
# Сканирование большего диапазона
python main.py --scan --scan-range 0xF180 0xF1FF

# Или попробуйте другие известные диапазоны
python main.py --scan --scan-range 0x2100 0x2110  # Производственные данные
python main.py --scan --scan-range 0xF400 0xF410  # Расширенные данные
```

---

## 📊 Пример успешного эксперимента

```
Шаг 1: python main.py --read-vin
✅ VIN: 1HD4LE218KC123456

Шаг 2: python main.py --scan
✅ DID 0xF192: 00012840 (4 байт)
   ➡️ 4-byte (BE): 7584.0 km с коэф. 0.1  ← СОВПАДАЕТ С ПАНЕЛЬЮ!

Шаг 3: python main.py --read-odometer 0xF192
✅ Одометр: 7584.0 km

Шаг 4: Сохранено в config.py:
ODOMETER_DID = 0xF192
ODOMETER_SCALE_FACTOR = 0.1
```

---

## 🆘 Если ничего не работает

1. **Проверьте железо:**
   - Индикатор на OpenPort 2.0 горит?
   - OBD-II порт правильно подключен?
   - Зажигание включено?

2. **Проверьте драйверы:**
   - Установлены ли драйверы OpenPort 2.0?
   - Определяется ли устройство в диспетчере устройств Windows?

3. **Попробуйте другой инструмент:**
   - Установите Tactrix EcuFlash и проверьте связь через него
   - Это подтвердит, что железо работает

4. **Включите подробные логи:**
   ```bash
   python main.py --read-vin --verbose
   ```
   Отправьте логи для анализа

---

**Удачи в экспериментах!** 🔬🏍️
